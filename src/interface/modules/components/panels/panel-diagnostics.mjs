/**
 * @file Tab panel definition with bindings for Diagnostics.
 */
import { html } from '/modules/hybrids.js';

export default styles => ({
  render: () => html`
    ${styles}

    Diagnostics Panel
  `,
});
