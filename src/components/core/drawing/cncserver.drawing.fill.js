/**
 * @file Code for drawing fill management.
 */
const { Path, Rectangle } = require('paper');

module.exports = (cncserver, drawing) => {
  const fill = (path, parent = null, bounds = null) => {

  };

  return fill;
};
