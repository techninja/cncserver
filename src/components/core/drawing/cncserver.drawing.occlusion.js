/**
 * @file Code for drawing occlusion path segmentation management.
 */
const { Path, Rectangle } = require('paper');

module.exports = (cncserver, drawing) => {
  const occlusion = (path, parent = null, bounds = null) => {

  };

  return occlusion;
};
